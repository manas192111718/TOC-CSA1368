#include <stdio.h>
#include <string.h>
void S(char input[]);
int belongsToLanguage(char input[])
{
  if (strlen(input) == 0)
  {
    return 1; 
  } 
  else if (input[0] == '0' && input[strlen(input)-1] == '0') 
  {
    S(input); 
    return 1;
  }
  return 0;
}
